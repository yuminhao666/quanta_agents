# Prompt Pack Builder

动态组装 Agent 输入，不维护一个写死的巨型 prompt。

Prompt Pack 由以下部分组成：

```text
system rules
role instruction
asset class framework
asset-specific framework
graph context
theme state
evidence capsule
output schema
```

写入目标：

```text
agent_workspace/prompt_packs
```
